spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:testdb;
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

decorator:
  datasource:
    p6spy:
      enable-logging: true

dutypark:
  slack:
    token: ""
    channel: "error-log"

server:
  ssl:
    enabled: false
  port: 8080
